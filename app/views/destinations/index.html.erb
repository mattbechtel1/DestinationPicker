<section class="container page-index">
  <h1>Destinations</h1>
  <ul class="destination-grid">
    <% @destinations.each do |destination| %>
      <li><%= render "destination_card", destination: destination %></li>
    <% end %>
  </ul>
</section>
